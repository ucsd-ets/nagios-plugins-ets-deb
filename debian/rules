#!/usr/bin/make -f
export DH_VERBOSE = 1
%:
	dh $@

override_dh_auto_install:
	echo hello
	pwd
	install -d debian/nagios-plugins-ets/usr/lib64/nagios/plugins
	#install -d debian/nagios-plugins-ets/usr/lib64/nagios/nagios-plugins-ets
	echo x > debian/check_memory
	# mkdir -p debian/nagios-plugins-ets/usr/lib64/nagios/plugins
	# echo x > debian/nagios-plugins-ets/usr/lib64/nagios/plugins/check_memory
	install -p -m0755 debian/check_memory debian/nagios-plugins-ets/usr/lib64/nagios/plugins/check_memory


#!/usr/bin/make -f
# You must remove unused comment lines for the released package.
#export DH_VERBOSE = 1

# %:
# /debian/nagios-plugins-ets/usr
